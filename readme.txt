Solution consists of 2 projects, ClientApp (Front-end) and LotteryApplication (API). 

The API must be run from VS (or built and run with dotnet executable).
For ClientApp, go to /ClientApp and run the following commands:

npm install
ng serve -o